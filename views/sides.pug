extends layout

block content
    h1 Sides
    each item in menu.sides
        div(class="menu_item")
            
            ul                
                li  #{item.name}
                img(src=item.image)            
                li #{item.description}
        br
        br